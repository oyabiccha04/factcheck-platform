
このサイトの前提まとめ（引継ぎ・憲法ドキュメント）
目的：この文書は「会話が長くなって前提が抜ける」問題を解決するために、前提（不変のルール／運用思想／サイト構造の大枠）をリポジトリ内に固定して保存するためのものです。Cursor/Codex/ChatGPTなど、どのAIを使っても この文書を参照すれば同じ前提で相談を継続できます。




━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
北極星（North Star）※最重要・確定
北極星（1行）：
「予測を、ファクトで評価し、的中率で可視化する。」
用途：

トップページのヒーローコピー
Aboutページの要約（冒頭）
プロダクト説明の冒頭
迷ったときの判断基準
意味（固定）：

予測（事前の言明）を、結果（一次情報）で照合し、検証可能な形で残す。
断罪する目的ではない。
予測と結果を混ぜず、照合の履歴そのものを残す。
✅ 確定（変更には本ドキュメントの改訂が必要）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. プロダクト憲法（編集・運用原則）※固定
【補足条項：コンテンツ位置づけと波及範囲（追加・確定）】

格闘技は本プロダクトにおける①コンテンツ（起点コンテンツ）である。
これは格闘技そのものを閉じたテーマとして扱うためではなく、予測
検証
事実の蓄積
という枠組みを、他分野へ拡張可能な形で実証するための最初の題材である。

本プロダクトは、将来的に以下の分野へ思想・手法を波及させることを否定しない。政治
経済
社会事象
その他「予測が発せられるが、検証されにくい領域」
ただし、分野を拡張する場合であっても、北極星（予測を、ファクトで評価する）および本憲法の原則は厳守する。感情的評価
思想的断罪
支持・不支持の表明
を主目的とするコンテンツ化は行わない。
【補足条項：収益化の基本方針（追加・確定）】

本プロダクトは、広告および寄付（ドネーション）による収益化を目指す。
収益化は、予測や結果の改変
特定の結論への誘導
スポンサー・支援者への迎合
を正当化する理由とはならない。

広告・寄付の導入にあたっても、予測と結果の分離
一次情報に基づく事実の保持
履歴の改変を行わないこと
という原則を最優先とする。

収益は、サイト運営
データ整備
長期的な履歴保存
を可能にするための手段であり、プロダクトの思想そのものではない。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【前文：思想背景（追加・確定）】
本プロダクトは、過去に検討された「The Forecast Check」を含む試行錯誤の中で、以下の問題意識に基づいて成立している。
世の中には、多くの「予測」が発せられているが、その多くは結果と照合されないまま消費・忘却されている。成功した予測や象徴的な的中例のみが記憶され、外れた予測・未検証の予測は記録に残らないという構造的偏りが存在する。
この偏りは、予測者の評価や断罪以前に、「事実としての履歴が存在しない」こと自体が問題である。
本プロダクトは、

予測を評価するためではなく、
人を格付けするためでもなく、
正しさを裁定するためでもない。
予測と結果を分離し、照合可能な形で記録し続けること——その行為そのものに価値がある、という立場を取る。
この思想は、**感情や権威ではなく、事実を見る態度（FACTFULNESS的態度）**と整合する。なお本プロダクトは、照合結果を **的中率** として可視化し、予測者別のランキング表示を行うことがある。ただし、断罪・誹謗・煽りを目的とした表現や設計は行わない。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0. このサイトは何か（1分で分かる説明）

このサイトは、格闘技（主にRIZIN/UFCなど）の試合・興行に関する予測と、その検証ログを蓄積し、後から検証できる形で公開することを目的とする。
重要なのは「当たった／外れた」だけではなく、いつ・どんな根拠で・どの形式で予測したか
結果が出た後に どう検証し、学びを更新したか
を、データとして残すこと。

サイトは GitHub Pages でホスティングしている。


1. 絶対に守る運用ルール（最重要）
1.1 ログの正本（唯一の真実）

セーブデータ v1.2.1（完全統合版・全文）が唯一の正本。
運用方針は 毎回「全文更新」。
差分更新・簡略化は禁止。
1.2 データ単位

原則：1試合 = 1 JSON（standard-log-v1.2-json）。
ただし、興行単位（例：RIZIN.20 など）でまとめる運用を採用する場合は、「1興行 = 1ファイル」 で統一し、中途半端な混在（試合単位と興行単位が混ざる）を避ける。
1.3 予測の扱い

主予測：勝者方向（誰が勝つか）
副次情報：KO / 判定 / SUB を補助情報として持たせる。
「何を予測として採用するか」「どこまでを副次情報とするか」は、この枠組みを崩さない。

1.3.1 予測者（誰が予測したか）の扱い（追加・確定）

予測は「誰が・いつ・何を」言ったかを一次情報として残す。表示上も、予測者（予測主体）の情報を予測データのメタ情報として扱い、イベント/試合単位で参照可能にする（未確定・未定義の場合は **「不明」** と明示する）。
ただし、断罪・誹謗・煽りを目的とした表現や設計は行わない（「予測↔事実」の照合履歴を追えることを優先する）。

1.3.2 予測ソース（出典）の必須化（追加・確定）

信頼性担保のため、予測には必ずソース（一次情報への参照）を記載する。最低限、次のいずれかを満たす：
- URL（動画/記事/投稿など）＋必要ならタイムスタンプや該当箇所のメモ
- リポジトリ内の参照（例：`statements[].statement_id` など、同一ファイル内で一次情報にたどれる識別子）
ソース不明の場合は、推測で埋めず **「不明」** と明示したうえで、後から一次情報に基づいて更新できる形にする。

1.3.3 予測メタ情報の最小キー（追加・確定）

MVP段階の表示・集計が参照する「予測のメタ情報」は、以下のキーを最小仕様として固定する（このキー名でデータを持つ）。

- 予測者（必須）
  - `predictor_name`: string（例：人名/媒体名/アカウント名）。未確定・未定義の場合は **"不明"** と明示する
- ソース（必須）
  - `source_url`: string（一次情報URL。ある場合はこれを優先）
  - `source_ref`: string（リポジトリ内参照。例：`statements[].statement_id=...`）
  - `source_note`: string（タイムスタンプ/該当箇所メモなど）
  - いずれも不明の場合は、推測で埋めず **"不明"** と明示する

補足：現状 `data/events/*.json` にはスキーマ混在があるため、ランキング（的中率）などの集計対象は「勝者方向が構造化されているデータ」に限定する。混在スキーマを無理に推測で統合しない。
1.4 集計のルール

予測者別集計：予測データに予測者（`predictor_name`）が明記されたものを対象とする（未確定・未定義は **"不明"** と明示して扱う）。
集計単位：興行クローズ時のみ（途中集計で数字を動かさない）。

1.4.1 ランキングの方針（追加・確定）

ランキングは **的中率** に基づく。集計・表示は「興行クローズ時のみ」の原則を維持し、途中経過で数値を動かさない。表示は事実指標としての可視化にとどめ、断罪・誹謗・煽りを目的としない。

1.4.2 的中率の定義（追加・確定）

的中率は **主予測（勝者方向）** の照合結果のみで計算する（副次＝KO/判定/SUB の一致は、必要に応じて別枠の参考情報として扱う）。
集計対象は、`prediction.exists = true` かつ結果（勝者）が確定しているものに限る。
1.5 会場スコープの優先順位

判定は 会場 → スコープ の順で決める。
一度決めたスコープ判定は 逆転不可。
1.6 「ドームクラス興行」の定義

ドームクラス = 東京ドーム開催のみ（独自定義）。
1.7 MVP段階での制約

MVP段階では indexes 作成禁止。


2. リポジトリ構造・ページ構造（分かっている範囲の前提）
※注意：この文書は現時点の会話ベースで整理しており、リポジトリの実ファイルをAIが直接参照できていない場合は、ファイル実体に合わせて後から更新する。
2.1 公開ページ

主要表示ページとして logs.html を使用する（優先）。

logs_v2.html が存在する場合でも、現運用の基準は logs.html。

2.1.1 インデックス（イベント一覧）の正本（追加・確定）

イベント一覧インデックスは `data/index.json` を唯一の正本として運用する（表示ページの参照先もここに統一する）。
2.2 公開URL（ユーザー申告ベース）

GitHub Pages 公開URL：https://oyabiccha04.github.io/factcheck-platform/logs_v2_adapter_test.html（動作確認で参照されたURL）
※「本番の入口URL（トップ）」と「ログ表示ページの正URL」は、リポジトリをCursorで開いて確定させ、ここに追記する。




3. データ配置ルール（JSONをどこに置くか）

原則：データは「表示ページが読み込める場所」に置く。※現時点ではリポジトリ実体を未確認のため、ここは 推奨の型 を先に固定する。
3.1 推奨ディレクトリ（提案）

data/：興行JSON・試合JSONの格納場所
master/：定義や参照データの格納場所
3.2 既に追加済みの前提（ユーザー運用）

Master Definition を  /master/master_definition.json  として追加済み。


4. サイトを「充実させる」とは何か（思想）
このサイトで「充実」とは、単にデータ量を増やすことではなく、次を満たすこと：
1) 初見の人が意味を理解できる（何のサイトか／何が見られるか） 2) 同じ基準で比較できる（興行・試合をまたいで形式が統一されている） 3) 検証できる（予測→結果→振り返りがつながる） 4) 更新が続く（運用が苦しくならない）


5. AI（Cursor/Codex/ChatGPT）に依頼するときの鉄則
5.1 依頼の基本形

大きな依頼を一気に出さない。
代わりに「小さな単位」に切って依頼する。
5.2 AIに必ず守らせること（コピペ用）
以下を、AIに最初に貼る（毎回）：

このリポジトリの前提は このサイトの前提まとめ（引継ぎ・憲法ドキュメント） に書かれている。
差分更新や簡略化は禁止。正本は全文更新。
データ形式は Standard Log v1.2系。
logs.html が基準ページ。


6. 直近の課題（これまでの詰まりポイント）

AIがGitHub上のファイルを直接読めず、logs.html / logs_v2.html のどちらが本番か
JSONをどこに置けば表示されるか
が前提ズレを起こして 表示が変わらない問題が発生した。

対策として、Cursor（リポジトリを開いた状態）で相談し、
この文書を「前提の固定先」にする。


7. 次にやること（前提が固まった後の進め方）

ここは「作業手順」ではなく、進行の順番だけ固定する。
1) Cursorでリポジトリを開き、実ファイル構造を確認 2) この文書の 2章（ページ構造）と 3章（データ配置）を、実態に合わせて確定 3) RIZIN/UFCの興行データを、確定した置き場に 順次追加 4) 表示ページ側（logs.html）が参照するパスを固定し、以降は迷わない


8. 追記欄（ここだけ更新していけばよい）

本番トップURL：
ログ表示ページURL：
JSON配置フォルダ（確定版）：
logs.html が読む「インデックス/マニフェスト」ファイル有無：
logs_v2.html の扱い（残す/消す/隔離）：

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. 直近の実装反映（2026-03-17 更新メモ）
※このセクションは「次のチャットで状況を即復元する」ための事実メモ。思想や固定ルールではない。

### 9.1 インデックスの正本
- **イベント一覧インデックス**は `data/index.json` を正とする
  - 形式：`{ "events": [ { title, date, organization, file, id? } ] }`
- `data/index.json` に **`rizin-40` / `the-match-2022`** を追加済み（一覧から到達可能）

### 9.2 主要ページの導線（現状）
- `index.html` は `data/index.json` を参照し、最新/ランダムを生成する
- `logs.html` は `data/index.json` を参照してカード一覧を描画する
- `event.html` は `event.html?file=xxx.json` を受け取り `data/events/xxx.json` を読み込む
  - `?file=` なしで開いた場合は、`data/index.json` から **最新イベント（最大10件）** をカードで表示し、そこから詳細に遷移できる

### 9.3 event.json のスキーマ混在への対応（表示側）
`data/events/*.json` は現状 2 系統が混在するが、`event.html` は両方を表示できるようにしている：
- **A: 興行/試合カード型**（例：RIZIN 20〜35）
  - `event_meta.event_name` / `event_meta.date`
  - `cards[].bouts[]`（`red`, `blue`, `outcome`）
  - `bouts[].prediction`（任意）を追加できる（下記）
- **B: 発言・予測ログ型**（例：the-match-2022）
  - `event_name` / `event_date`
  - `statements[]`（`prediction` と `verification` を表示）
  - （追加・運用メモ）`fights[]` を併設できる（カード/結果の表示用）
    - `fights[].red` / `fights[].blue` / `fights[].winner` / `fights[].method` / `fights[].result`
    - （任意）`fights[].rules`（例：`round_minutes` / `rounds` / `extension_rounds` / `weight_class`）

### 9.4 予測の最小スキーマ（試合カード型）
北極星「予測を、ファクトで評価する」を画面上で体感できるよう、試合カード型でも予測を持てるようにした：
- 追加場所：`cards[].bouts[].prediction`
- 形式（最小）：
  - `exists: true`
  - `expected_winner`（主予測＝勝者方向）
  - `expected_method`（副次＝KO/判定/SUB）
  - `basis`（任意：根拠のメモ）
- 表示：`event.html` は予測枠を必ず出し、未登録は薄く表示。結果がある場合は **一致/不一致（照合）** を軽く表示する（人の格付け・ランキングはしない）

### 9.4.1 event.html の表示方針（2026-03-17 追記・運用メモ）
- 表示順（大会ページ）：**試合一覧（カード→結果→予測）→ 発言・予測と検証**
- 試合一覧の並び：`bout_id` を手掛かりに **メイン→降順→オープニング**（例：`*-main` → `*-14` → … → `*-opening`）
- statements の差し込み：
  - `statements[].prediction.target` に **両選手名（red/blue）が含まれる**ものだけを、そのカード直下に表示（推測で紐付けない）
  - 紐付けできない statement は「その他」に残す
- 読み物としての表示：
  - `content.raw_text` がカギ括弧つき引用で、`content.normalized_text` が言い換えに留まる場合は **引用（raw）を主表示**
  - 文脈補足がある場合は `normalized_text` を主表示し、必要に応じて `raw_text` を「引用」として併記

### 9.5 今後の作業優先順位（運用メモ）
1) **格闘技のUI/UX最低限**（event/logs/格闘技トップの見た目と導線を揃える）
2) **データ追加**（予測データを増やす、the-match 型イベントを増やす）
3) 表記ゆれ対策（選手名の漢字/ローマ字混在は公開時に日本語表示へ寄せる。データ保持と表示名を分離する辞書方式を検討）


付録A：この文書の置き場所（推奨）

リポジトリ直下に PROJECT_CHARTER.md として配置（推奨）。
あるいは README が肥大化するなら docs/PROJECT_CHARTER.md。

付録B：開発判断用の整理ドキュメント

目的・設計思想・優先すること・やってはいけないことを開発判断に使えるレベルで整理した文書：
**docs/DEVELOPMENT_GUIDELINES.md** を参照のこと。

付録C：Cursorに最初に投げる「確認プロンプト」（コピペ用）

このリポジトリにある「PROJECT_CHARTER.md（サイト前提まとめ）」を前提として扱ってください。
次を確認して、事実ベースで説明してください。
1) logs.html はどのJSON/フォルダを読んでいる？
2) logs_v2.html は現状使われている？
3) JSONはどのフォルダに置けば表示される？（実コード参照で）
4) 公開URL（GitHub Pages）の入口はどれ？
結論だけでなく、どのファイルを根拠にそう言えるかも示してください。
